const founderPhoto=document.querySelector('.photo-frame img');if(founderPhoto){const parts=['assets/founder-b64-1.txt','assets/founder-b64-2.txt','assets/founder-b64-3.txt','assets/founder-b64-4.txt'];founderPhoto.loading='eager';founderPhoto.fetchPriority='high';founderPhoto.decoding='async';founderPhoto.removeAttribute('srcset');founderPhoto.style.opacity='0';founderPhoto.style.transition='opacity .2s ease';founderPhoto.removeAttribute('src');Promise.all(parts.map(url=>fetch(url+'?v=7').then(response=>{if(!response.ok)throw new Error('No se pudo cargar '+url);return response.text()}))).then(chunks=>{const base64=chunks.join('').replace(/\s/g,'');if(base64.length!==7184)throw new Error('Foto incompleta: '+base64.length);founderPhoto.onload=()=>{founderPhoto.style.opacity='1'};founderPhoto.src='data:image/webp;base64,'+base64}).catch(()=>{founderPhoto.style.opacity='0'})}const header=document.querySelector('.site-header');const menuButton=document.querySelector('.menu-toggle');const nav=document.querySelector('.site-nav');const navLinks=document.querySelectorAll('.site-nav a');const setHeader=()=>header.classList.toggle('scrolled',window.scrollY>16);setHeader();window.addEventListener('scroll',setHeader,{passive:true});menuButton?.addEventListener('click',()=>{const open=!nav.classList.contains('open');nav.classList.toggle('open',open);menuButton.classList.toggle('open',open);menuButton.setAttribute('aria-expanded',String(open));menuButton.setAttribute('aria-label',open?'Cerrar menú':'Abrir menú')});navLinks.forEach(link=>link.addEventListener('click',()=>{nav.classList.remove('open');menuButton?.classList.remove('open');menuButton?.setAttribute('aria-expanded','false')}));document.addEventListener('keydown',event=>{if(event.key==='Escape'&&nav.classList.contains('open')){nav.classList.remove('open');menuButton?.classList.remove('open');menuButton?.setAttribute('aria-expanded','false');menuButton?.focus()}});const revealItems=document.querySelectorAll('.reveal');if('IntersectionObserver'in window&&!window.matchMedia('(prefers-reduced-motion: reduce)').matches){const observer=new IntersectionObserver((entries,obs)=>{entries.forEach(entry=>{if(entry.isIntersecting){entry.target.classList.add('visible');obs.unobserve(entry.target)}})},{threshold:.12,rootMargin:'0px 0px -40px'});revealItems.forEach(item=>observer.observe(item))}else{revealItems.forEach(item=>item.classList.add('visible'))}document.getElementById('year').textContent=new Date().getFullYear();